jib {
    from {
        image = 'openjdk:11'
    }
    to {
        image = 'thaneesh/jibtest'
    }
    container {
        jvmFlags = ['-Xms512m', '-Xdebug']
        mainClass = 'com.example.JibTest.JibTestApplication'
        args = []
        ports = ['8080/tcp']
    }
}